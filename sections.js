// sections.js - Carga de contenido específico por sección

async function loadHelpContent() {
    const section = document.getElementById('help');
    if (!section) return;
    
    // Tu contenido existente para la sección de ayuda...
}

async function loadDownloadsContent() {
    const section = document.getElementById('downloads');
    if (!section) return;
    
    // Tu contenido existente para descargas...
}

// ... y así para cada sección